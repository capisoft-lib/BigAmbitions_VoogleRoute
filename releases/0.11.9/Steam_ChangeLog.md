Unified vanilla-style UI — GPS panel, settings, bookmarks, history, and popups rebuilt on the bundled LIB BA Unified UI library (no extra Workshop mod)
City-map DRIVE / WALK shortcuts — beside the vanilla set-destination button when you select a building on the map (M)
Auto-enter on arrival — optional mod toggle to enter a vehicle or building when navigation reaches the destination
Delivery job routing — follows vanilla job guider targets when no map GPS destination is set
Auto-drive fuel estimate — confirmation popup shows approximate fuel for the trip
Bridge-deck teleport fix — auto-drive placement respects entrance height under elevated roads
Stuck fade recovery — clears a black screen overlay when no travel fade or subway ride is active
Display toggles — show or hide Voogle Route outside and inside buildings (ESC > Options > Mod)
Locales — arrival toast, subway hints, fuel line, and map shortcut strings in all 22 languages
