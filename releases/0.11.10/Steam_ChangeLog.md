Per-save mod options — route colors and ESC > Options > Mod toggles stored in your save file (per character/save slot)
Per-save bookmarks and visit history — bookmarks, quick shortcuts, and history travel with each save; auto-migration from legacy bookmarks.json
Route color persistence fix — foot, vehicle, and indoor line colors save correctly again (IL2CPP-safe storage)
Separate route colors — on-foot, vehicle, and indoor route lines each keep their own color per save
config.json dev-only — logging and debug flags stay in the mod folder; gameplay options are no longer shared across saves
Navigation polish — improved delivery-job destination sync, building auto-enter, and movement-mode detection
