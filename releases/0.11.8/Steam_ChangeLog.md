On-foot subway routing — when walkable NavMesh cannot reach your destination, Voogle Route can plan a route via subway (walk → ride → walk)
Auto-walk subway — at the board station, auto-walk triggers the ride and selects your exit station automatically (no manual map pick)
Use subway option — ESC > Options > Mod > Voogle Route: toggle subway fallback on or off (default on)
Subway route line — dashed yellow segment on the ground and on the city map (M); foot legs stay blue with no line drawn across the subway section
Map UI during subway — Voogle Route panels hide in subway mode so they no longer block station selection
Subway selection fix — bookmarks panel no longer disabled picking a subway destination on the map
Subway line elevation — subway trace uses the same ground height as the on-foot path on the city map
