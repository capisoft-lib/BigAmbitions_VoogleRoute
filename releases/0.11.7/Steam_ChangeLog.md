Visit History — scrollable list of your last 50 buildings visited (any type), most recent first
History shortcuts — clock icon on the GPS panel header and on the city-map BOOKMARKS panel opens History
Row actions — CENTER, ADD (save as bookmark), and SET destination, same layout as bookmark rows
Persistence — visit history stored in bookmarks.json (deduplicated by address/position)
GPS header — history icon added alongside bookmark, last-car, and settings buttons
Map polish — bookmarks stay visible when clicking buildings; hide only when opening BizMan or other map overlays
History on city map — panel opens from the bookmarks header while Voogle Maps is open
Business creation — company name field keeps focus while typing on the city map
Panel headers — BOOKMARKS and HISTORY headers aligned with the panel frame on the city map
